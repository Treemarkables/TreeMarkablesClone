{
  "account_id": "215-149-0929",
  "currency": "NZD",
  "timezone": "Pacific/Auckland",
  "location_targeting": {
    "radius_km": 15,
    "cities": ["Gisborne"]
  },
  "campaigns": [
    {
      "name": "Treemarkables - Tree Removal",
      "daily_budget": 18.00,
      "status": "ENABLED",
      "campaign_type": "SEARCH",
      "ad_network": "GOOGLE_SEARCH",
      "description": "High-intent tree removal searches in Gisborne area",
      "bid_strategy": "MAXIMIZE_CONVERSIONS",
      "bid_adjustments": {
        "schedule": [
          {
            "day_of_week": "MONDAY",
            "start_hour": 7,
            "end_hour": 17,
            "bid_modifier": 1.2
          },
          {
            "day_of_week": "TUESDAY",
            "start_hour": 7,
            "end_hour": 17,
            "bid_modifier": 1.2
          },
          {
            "day_of_week": "WEDNESDAY",
            "start_hour": 7,
            "end_hour": 17,
            "bid_modifier": 1.2
          },
          {
            "day_of_week": "THURSDAY",
            "start_hour": 7,
            "end_hour": 17,
            "bid_modifier": 1.2
          },
          {
            "day_of_week": "FRIDAY",
            "start_hour": 7,
            "end_hour": 17,
            "bid_modifier": 1.2
          }
        ]
      },
      "keywords": [
        {
          "text": "tree removal Gisborne",
          "match_type": "EXACT",
          "bid_amount": 3.50
        },
        {
          "text": "tree removal near me",
          "match_type": "PHRASE",
          "bid_amount": 3.20
        },
        {
          "text": "remove tree Gisborne",
          "match_type": "EXACT",
          "bid_amount": 3.00
        },
        {
          "text": "tree felling Gisborne",
          "match_type": "EXACT",
          "bid_amount": 2.80
        },
        {
          "text": "hazardous tree removal",
          "match_type": "PHRASE",
          "bid_amount": 3.50
        },
        {
          "text": "large tree removal",
          "match_type": "PHRASE",
          "bid_amount": 2.90
        },
        {
          "text": "dead tree removal",
          "match_type": "PHRASE",
          "bid_amount": 3.00
        },
        {
          "text": "tree removal cost",
          "match_type": "BROAD",
          "bid_amount": 2.00
        }
      ],
      "negative_keywords": [
        "DIY",
        "myself",
        "cheap",
        "free"
      ],
      "ads": [
        {
          "headline_1": "Professional Tree Removal Gisborne",
          "headline_2": "Certified Arborists | Fully Insured",
          "headline_3": "✓ Work Guaranteed | Safe Finish",
          "description": "Hazardous or overgrown trees? Treemarkables removes trees safely with a guaranteed finish. Qualified crew, modern equipment, debris cleanup included. Free quote.",
          "final_url": "https://treemarkables.co.nz/tree-removal"
        },
        {
          "headline_1": "Tree Removal Today - Gisborne",
          "headline_2": "Same-Day Quotes | Work Guaranteed",
          "headline_3": "Dead/Fallen Trees? We're Ready",
          "description": "Dangerous tree threatening your home? Fast removal, all work guaranteed. Emergency response available. Fully insured, professional cleanup.",
          "final_url": "https://treemarkables.co.nz/tree-removal"
        }
      ]
    },
    {
      "name": "Treemarkables - Tree Pruning",
      "daily_budget": 13.00,
      "status": "ENABLED",
      "campaign_type": "SEARCH",
      "ad_network": "GOOGLE_SEARCH",
      "description": "Tree pruning and maintenance demand in Gisborne",
      "bid_strategy": "MAXIMIZE_CONVERSIONS",
      "bid_adjustments": {
        "schedule": [
          {
            "day_of_week": "MONDAY",
            "start_hour": 8,
            "end_hour": 16,
            "bid_modifier": 1.15
          },
          {
            "day_of_week": "TUESDAY",
            "start_hour": 8,
            "end_hour": 16,
            "bid_modifier": 1.15
          },
          {
            "day_of_week": "WEDNESDAY",
            "start_hour": 8,
            "end_hour": 16,
            "bid_modifier": 1.15
          },
          {
            "day_of_week": "THURSDAY",
            "start_hour": 8,
            "end_hour": 16,
            "bid_modifier": 1.15
          },
          {
            "day_of_week": "FRIDAY",
            "start_hour": 8,
            "end_hour": 16,
            "bid_modifier": 1.15
          }
        ]
      },
      "keywords": [
        {
          "text": "tree pruning Gisborne",
          "match_type": "EXACT",
          "bid_amount": 2.50
        },
        {
          "text": "tree trimming Gisborne",
          "match_type": "EXACT",
          "bid_amount": 2.40
        },
        {
          "text": "tree pruning near me",
          "match_type": "PHRASE",
          "bid_amount": 2.20
        },
        {
          "text": "branch removal",
          "match_type": "PHRASE",
          "bid_amount": 1.80
        },
        {
          "text": "crown reduction",
          "match_type": "EXACT",
          "bid_amount": 2.10
        },
        {
          "text": "dead wooding",
          "match_type": "EXACT",
          "bid_amount": 2.00
        },
        {
          "text": "professional tree trimming",
          "match_type": "PHRASE",
          "bid_amount": 2.30
        }
      ],
      "negative_keywords": [
        "DIY",
        "myself",
        "cheap"
      ],
      "ads": [
        {
          "headline_1": "Professional Tree Pruning - Gisborne",
          "headline_2": "Healthy Trees | Results Guaranteed",
          "headline_3": "Expert Arborists On Your Side",
          "description": "Keep your trees healthy & safe. Expert pruning removes dead wood, improves structure. All work guaranteed. Free consultation.",
          "final_url": "https://treemarkables.co.nz/tree-pruning"
        },
        {
          "headline_1": "Shape Your Garden - Tree Trimming",
          "headline_2": "Professional Results | Guaranteed",
          "headline_3": "Transform Overgrown Trees",
          "description": "Overgrown trees spoiling your view? Expert trimming & shaping guaranteed. Quick cleanup, competitive rates, free quote.",
          "final_url": "https://treemarkables.co.nz/tree-pruning"
        }
      ]
    },
    {
      "name": "Treemarkables - Stump Grinding",
      "daily_budget": 9.00,
      "status": "ENABLED",
      "campaign_type": "SEARCH",
      "ad_network": "GOOGLE_SEARCH",
      "description": "Niche stump grinding service targeting existing stumps",
      "bid_strategy": "MAXIMIZE_CONVERSIONS",
      "bid_adjustments": {
        "schedule": []
      },
      "keywords": [
        {
          "text": "stump grinding Gisborne",
          "match_type": "EXACT",
          "bid_amount": 2.00
        },
        {
          "text": "stump removal",
          "match_type": "PHRASE",
          "bid_amount": 1.70
        },
        {
          "text": "stump grinder",
          "match_type": "EXACT",
          "bid_amount": 1.50
        },
        {
          "text": "remove tree stump",
          "match_type": "PHRASE",
          "bid_amount": 1.80
        },
        {
          "text": "stump removal cost",
          "match_type": "BROAD",
          "bid_amount": 1.20
        }
      ],
      "negative_keywords": [
        "DIY",
        "cheap"
      ],
      "ads": [
        {
          "headline_1": "Stump Grinding - Gisborne",
          "headline_2": "Complete Removal | Work Guaranteed",
          "headline_3": "Safety ✓ Professional ✓",
          "description": "Old tree stumps? We grind them below ground. Guaranteed results—your yard clean & hazard-free. Perfect for replanting or landscaping. Free estimate.",
          "final_url": "https://treemarkables.co.nz/stump-grinding"
        },
        {
          "headline_1": "Tree Removal + Stump Grinding",
          "headline_2": "Complete Package | Guaranteed",
          "headline_3": "No Stump Left Behind",
          "description": "Remove the tree, grind the stump. All work guaranteed. One-stop cleanup, minimal disruption.",
          "final_url": "https://treemarkables.co.nz/stump-grinding"
        }
      ]
    },
    {
      "name": "Treemarkables - Hedge Trimming",
      "daily_budget": 12.00,
      "status": "ENABLED",
      "campaign_type": "SEARCH",
      "ad_network": "GOOGLE_SEARCH",
      "description": "Regular hedge trimming and maintenance services",
      "bid_strategy": "MAXIMIZE_CONVERSIONS",
      "bid_adjustments": {
        "schedule": [
          {
            "day_of_week": "FRIDAY",
            "start_hour": 15,
            "end_hour": 23,
            "bid_modifier": 1.1
          },
          {
            "day_of_week": "SATURDAY",
            "start_hour": 0,
            "end_hour": 23,
            "bid_modifier": 1.1
          },
          {
            "day_of_week": "SUNDAY",
            "start_hour": 0,
            "end_hour": 17,
            "bid_modifier": 1.1
          }
        ]
      },
      "keywords": [
        {
          "text": "hedge trimming Gisborne",
          "match_type": "EXACT",
          "bid_amount": 1.80
        },
        {
          "text": "hedge cutting",
          "match_type": "PHRASE",
          "bid_amount": 1.60
        },
        {
          "text": "shrub trimming",
          "match_type": "PHRASE",
          "bid_amount": 1.50
        },
        {
          "text": "hedge maintenance",
          "match_type": "EXACT",
          "bid_amount": 1.70
        },
        {
          "text": "professional hedge trimming",
          "match_type": "PHRASE",
          "bid_amount": 1.90
        }
      ],
      "negative_keywords": [
        "DIY",
        "cheap"
      ],
      "ads": [
        {
          "headline_1": "Regular Hedge Trimming - Gisborne",
          "headline_2": "Healthy Hedges | Guaranteed Results",
          "headline_3": "Professional Maintenance Service",
          "description": "Overgrown hedges? Regular trimming keeps them healthy & neat. Guaranteed quality. Seasonal maintenance, competitive rates.",
          "final_url": "https://treemarkables.co.nz/hedge-trimming"
        },
        {
          "headline_1": "Tidy Hedges = Beautiful Property",
          "headline_2": "Professional Cutting | Guaranteed",
          "headline_3": "Transform Your Garden",
          "description": "Messy hedges dragging down your property? Expert trimming guaranteed. Clean finish, quick turnaround.",
          "final_url": "https://treemarkables.co.nz/hedge-trimming"
        }
      ]
    },
    {
      "name": "Treemarkables - Emergency Tree Response",
      "daily_budget": 8.00,
      "status": "ENABLED",
      "campaign_type": "SEARCH",
      "ad_network": "GOOGLE_SEARCH",
      "description": "Storm damage and emergency tree removal - high urgency",
      "bid_strategy": "MAXIMIZE_CONVERSIONS",
      "bid_adjustments": {
        "schedule": [
          {
            "day_of_week": "MONDAY",
            "start_hour": 17,
            "end_hour": 23,
            "bid_modifier": 1.3
          },
          {
            "day_of_week": "TUESDAY",
            "start_hour": 17,
            "end_hour": 23,
            "bid_modifier": 1.3
          },
          {
            "day_of_week": "WEDNESDAY",
            "start_hour": 17,
            "end_hour": 23,
            "bid_modifier": 1.3
          },
          {
            "day_of_week": "THURSDAY",
            "start_hour": 17,
            "end_hour": 23,
            "bid_modifier": 1.3
          },
          {
            "day_of_week": "FRIDAY",
            "start_hour": 17,
            "end_hour": 23,
            "bid_modifier": 1.3
          },
          {
            "day_of_week": "SATURDAY",
            "start_hour": 0,
            "end_hour": 23,
            "bid_modifier": 1.3
          },
          {
            "day_of_week": "SUNDAY",
            "start_hour": 0,
            "end_hour": 23,
            "bid_modifier": 1.3
          }
        ]
      },
      "keywords": [
        {
          "text": "emergency tree removal",
          "match_type": "EXACT",
          "bid_amount": 4.00
        },
        {
          "text": "fallen tree removal",
          "match_type": "EXACT",
          "bid_amount": 3.80
        },
        {
          "text": "storm damage removal",
          "match_type": "PHRASE",
          "bid_amount": 3.50
        },
        {
          "text": "tree damage",
          "match_type": "PHRASE",
          "bid_amount": 2.50
        },
        {
          "text": "emergency tree service",
          "match_type": "PHRASE",
          "bid_amount": 3.20
        },
        {
          "text": "24 hour tree removal",
          "match_type": "PHRASE",
          "bid_amount": 3.00
        }
      ],
      "negative_keywords": [
        "DIY",
        "cheap"
      ],
      "ads": [
        {
          "headline_1": "Emergency Tree Removal - Now!",
          "headline_2": "Fast Response | Work Guaranteed",
          "headline_3": "Fallen Trees? We're Ready",
          "description": "Storm damage? Fallen tree blocking your driveway? All work guaranteed. Fast response, safe removal, full cleanup. Call now.",
          "final_url": "https://treemarkables.co.nz/emergency-tree-removal"
        },
        {
          "headline_1": "Don't Risk DIY Tree Removal",
          "headline_2": "Professional Service | Guaranteed",
          "headline_3": "Safe | Insured | Qualified",
          "description": "Hazardous tree threatening your home? Trust qualified arborists. Work guaranteed. Fast assessment, safe removal, insurance-friendly documentation.",
          "final_url": "https://treemarkables.co.nz/emergency-tree-removal"
        }
      ]
    }
  ]
}

#!/usr/bin/env python3
"""
Treemarkables Google Ads API - Campaign Deployment
Reads campaigns.json and syncs to Google Ads account via API.
Smart update logic: only modifies changed campaigns, preserves history.

Usage:
  python3 deploy_campaigns.py            # Dry run (shows what would change)
  python3 deploy_campaigns.py --confirm  # Actually deploy changes
"""

import json
import os
import sys
import argparse
import logging
from pathlib import Path
from datetime import datetime
from dotenv import load_dotenv

# Google Ads API client
from google.ads.googleads.client import GoogleAdsClient
from google.ads.googleads.errors import GoogleAdsException

# Load environment variables from .env
load_dotenv()

# ============================================================================
# CONFIGURATION
# ============================================================================

CREDENTIALS = {
    "client_id": os.getenv("GOOGLE_ADS_CLIENT_ID"),
    "client_secret": os.getenv("GOOGLE_ADS_CLIENT_SECRET"),
    "refresh_token": os.getenv("GOOGLE_ADS_REFRESH_TOKEN"),
    "developer_token": "hKuaqYP3FzeXGdSM_p7uMA",
}

CUSTOMER_ID = "215149092"  # Without dashes for API calls

CONFIG_FILE = Path("campaigns.json")

# ============================================================================
# HELPERS
# ============================================================================

def log(message, level="INFO"):
    """Simple logging with timestamps"""
    timestamp = datetime.now().strftime("%Y-%m-%d %H:%M:%S")
    prefix = f"[{timestamp}] {level:8}"
    color_codes = {
        "INFO": "\033[94m",
        "SUCCESS": "\033[92m",
        "WARNING": "\033[93m",
        "ERROR": "\033[91m",
        "RESET": "\033[0m"
    }
    color = color_codes.get(level, "")
    reset = color_codes["RESET"]
    print(f"{color}{prefix}{reset} {message}")

def load_config():
    """Load campaigns.json configuration"""
    if not CONFIG_FILE.exists():
        log(f"ERROR: {CONFIG_FILE} not found!", level="ERROR")
        sys.exit(1)
    
    with open(CONFIG_FILE, "r") as f:
        config = json.load(f)
    
    log(f"Loaded config with {len(config['campaigns'])} campaigns")
    return config

def verify_credentials():
    """Verify that all required credentials are present"""
    required = ["client_id", "client_secret", "refresh_token", "developer_token"]
    missing = [k for k in required if not CREDENTIALS.get(k)]
    
    if missing:
        log(f"ERROR: Missing credentials: {missing}", level="ERROR")
        log("Run setup_oauth.py first to generate credentials", level="ERROR")
        sys.exit(1)
    
    log("✓ Credentials verified", level="SUCCESS")
    return True

def init_google_ads_client():
    """Initialize Google Ads API client"""
    try:
        client = GoogleAdsClient.load_from_dict(
            {
                "developer_token": CREDENTIALS["developer_token"],
                "client_id": CREDENTIALS["client_id"],
                "client_secret": CREDENTIALS["client_secret"],
                "refresh_token": CREDENTIALS["refresh_token"],
                "use_proto_plus": True,
            }
        )
        log("✓ Google Ads API client initialized", level="SUCCESS")
        return client
    except Exception as e:
        log(f"ERROR: Failed to initialize Google Ads client: {e}", level="ERROR")
        sys.exit(1)

# ============================================================================
# CAMPAIGN OPERATIONS
# ============================================================================

class CampaignManager:
    """Manages campaign creation and updates via Google Ads API"""
    
    def __init__(self, client, customer_id):
        self.client = client
        self.customer_id = customer_id
        self.campaign_service = client.get_service("CampaignService")
        self.ad_group_service = client.get_service("AdGroupService")
        self.ad_group_ad_service = client.get_service("AdGroupAdService")
        self.ad_group_criterion_service = client.get_service("AdGroupCriterionService")
        self.changes = {"created": 0, "updated": 0, "errors": 0}
    
    def get_existing_campaigns(self):
        """Fetch all existing campaigns in the account"""
        query = """
            SELECT campaign.id, campaign.name, campaign.status, campaign.budget
            FROM campaign
            ORDER BY campaign.name
        """
        
        try:
            response = self.campaign_service.search_stream(
                customer_id=self.customer_id,
                query=query
            )
            
            campaigns = {}
            for batch in response:
                for row in batch.results:
                    name = row.campaign.name
                    campaigns[name] = {
                        "id": row.campaign.id,
                        "status": row.campaign.status.name,
                        "budget_micros": row.campaign.budget.amount_micros,
                        "resource_name": row.campaign.resource_name,
                    }
            
            log(f"✓ Retrieved {len(campaigns)} existing campaigns", level="SUCCESS")
            return campaigns
        except GoogleAdsException as e:
            log(f"ERROR: Failed to fetch campaigns: {e}", level="ERROR")
            return {}
    
    def create_campaign(self, campaign_spec, dry_run=True):
        """Create new campaign"""
        campaign_name = campaign_spec["name"]
        daily_budget_micros = int(campaign_spec["daily_budget"] * 1_000_000)
        
        campaign = self.client.get_type("Campaign")
        campaign.name = campaign_name
        campaign.status = self.client.enums.CampaignStatusEnum.ENABLED
        campaign.advertising_channel_type = (
            self.client.enums.AdvertisingChannelTypeEnum.SEARCH
        )
        
        # Budget
        budget = self.client.get_type("Budget")
        budget.amount_micros = daily_budget_micros
        campaign.budget = budget
        
        # Bidding strategy
        campaign.bidding_strategy_type = (
            self.client.enums.BiddingStrategyTypeEnum.MAXIMIZE_CONVERSIONS
        )
        
        # Geo-targeting: Gisborne, NZ
        geo_target = campaign.geo_targets.add()
        geo_target.id = 2087
        
        # Network settings
        campaign.network_settings.target_google_search = True
        campaign.network_settings.target_content_network = False
        
        # Create operation
        operation = self.client.get_type("CampaignOperation")
        operation.create.CopyFrom(campaign)
        
        if dry_run:
            log(f"  [DRY RUN] Campaign: {campaign_name} (${campaign_spec['daily_budget']}/day)")
            return None
        
        try:
            response = self.campaign_service.mutate_campaigns(
                customer_id=self.customer_id,
                operations=[operation]
            )
            
            resource_name = response.results[0].resource_name
            log(f"  ✓ Created: {campaign_name}", level="SUCCESS")
            self.changes["created"] += 1
            return resource_name
        except GoogleAdsException as e:
            log(f"  ERROR: {campaign_name}: {e}", level="ERROR")
            self.changes["errors"] += 1
            return None
    
    def get_ad_groups_for_campaign(self, campaign_resource_name):
        """Fetch ad groups for a campaign"""
        query = f"""
            SELECT ad_group.id, ad_group.name, ad_group.resource_name
            FROM ad_group
            WHERE ad_group.campaign = '{campaign_resource_name}'
        """
        
        try:
            response = self.ad_group_service.search_stream(
                customer_id=self.customer_id,
                query=query
            )
            
            ad_groups = {}
            for batch in response:
                for row in batch.results:
                    ad_groups[row.ad_group.name] = {
                        "resource_name": row.ad_group.resource_name,
                        "id": row.ad_group.id,
                    }
            return ad_groups
        except:
            return {}
    
    def create_ad_group(self, campaign_resource_name, campaign_name, dry_run=True):
        """Create ad group for campaign"""
        ad_group_name = f"{campaign_name} - Ads"
        
        ad_group = self.client.get_type("AdGroup")
        ad_group.name = ad_group_name
        ad_group.campaign = campaign_resource_name
        ad_group.status = self.client.enums.AdGroupStatusEnum.ENABLED
        ad_group.cpc_bid_micros = int(2.0 * 1_000_000)
        
        operation = self.client.get_type("AdGroupOperation")
        operation.create.CopyFrom(ad_group)
        
        if dry_run:
            return None
        
        try:
            response = self.ad_group_service.mutate_ad_groups(
                customer_id=self.customer_id,
                operations=[operation]
            )
            return response.results[0].resource_name
        except GoogleAdsException as e:
            log(f"    ERROR creating ad group: {e}", level="ERROR")
            return None
    
    def create_ads(self, ad_group_resource_name, ad_specs, dry_run=True):
        """Create responsive search ads"""
        operations = []
        
        for ad_spec in ad_specs:
            ad_group_ad = self.client.get_type("AdGroupAd")
            ad_group_ad.ad_group = ad_group_resource_name
            ad_group_ad.status = self.client.enums.AdGroupAdStatusEnum.ENABLED
            
            # Responsive Search Ad
            ad = self.client.get_type("Ad")
            ad.final_urls.append(ad_spec["final_url"])
            
            rsa = self.client.get_type("ResponsiveSearchAdInfo")
            
            # Headlines
            for headline_key in ["headline_1", "headline_2", "headline_3"]:
                headline = self.client.get_type("AdTextAsset")
                headline.text = ad_spec[headline_key]
                rsa.headlines.append(headline)
            
            # Descriptions
            for desc in [ad_spec["description"]]:
                description = self.client.get_type("AdTextAsset")
                description.text = desc
                rsa.descriptions.append(description)
            
            ad.responsive_search_ad.CopyFrom(rsa)
            ad_group_ad.ad.CopyFrom(ad)
            
            operation = self.client.get_type("AdGroupAdOperation")
            operation.create.CopyFrom(ad_group_ad)
            operations.append(operation)
        
        if dry_run or not operations:
            return
        
        try:
            response = self.ad_group_ad_service.mutate_ad_group_ads(
                customer_id=self.customer_id,
                operations=operations
            )
            log(f"    ✓ Created {len(response.results)} ads", level="SUCCESS")
        except GoogleAdsException as e:
            log(f"    ERROR creating ads: {e}", level="ERROR")
    
    def create_keywords(self, ad_group_resource_name, keywords, dry_run=True):
        """Create keywords for ad group"""
        operations = []
        
        match_type_enum = {
            "EXACT": self.client.enums.KeywordMatchTypeEnum.EXACT,
            "PHRASE": self.client.enums.KeywordMatchTypeEnum.PHRASE,
            "BROAD": self.client.enums.KeywordMatchTypeEnum.BROAD,
        }
        
        for kw_spec in keywords:
            keyword = self.client.get_type("KeywordInfo")
            keyword.text = kw_spec["text"]
            keyword.match_type = match_type_enum.get(
                kw_spec.get("match_type", "BROAD"),
                self.client.enums.KeywordMatchTypeEnum.BROAD
            )
            
            ad_group_criterion = self.client.get_type("AdGroupCriterion")
            ad_group_criterion.ad_group = ad_group_resource_name
            ad_group_criterion.status = self.client.enums.AdGroupCriterionStatusEnum.ENABLED
            ad_group_criterion.keyword.CopyFrom(keyword)
            
            # Bid
            if "bid_amount" in kw_spec:
                ad_group_criterion.cpc_bid_micros = int(kw_spec["bid_amount"] * 1_000_000)
            
            operation = self.client.get_type("AdGroupCriterionOperation")
            operation.create.CopyFrom(ad_group_criterion)
            operations.append(operation)
        
        if dry_run or not operations:
            return
        
        try:
            response = self.ad_group_criterion_service.mutate_ad_group_criteria(
                customer_id=self.customer_id,
                operations=operations
            )
            log(f"    ✓ Created {len(response.results)} keywords", level="SUCCESS")
        except GoogleAdsException as e:
            log(f"    ERROR creating keywords: {e}", level="ERROR")
    
    def create_negative_keywords(self, ad_group_resource_name, neg_keywords, dry_run=True):
        """Create negative keywords"""
        operations = []
        
        for neg_kw in neg_keywords:
            keyword = self.client.get_type("KeywordInfo")
            keyword.text = neg_kw
            keyword.match_type = self.client.enums.KeywordMatchTypeEnum.BROAD
            
            ad_group_criterion = self.client.get_type("AdGroupCriterion")
            ad_group_criterion.ad_group = ad_group_resource_name
            ad_group_criterion.keyword.CopyFrom(keyword)
            ad_group_criterion.negative = True
            
            operation = self.client.get_type("AdGroupCriterionOperation")
            operation.create.CopyFrom(ad_group_criterion)
            operations.append(operation)
        
        if dry_run or not operations:
            return
        
        try:
            response = self.ad_group_criterion_service.mutate_ad_group_criteria(
                customer_id=self.customer_id,
                operations=operations
            )
            log(f"    ✓ Created {len(response.results)} negative keywords", level="SUCCESS")
        except GoogleAdsException as e:
            log(f"    WARNING: Negative keywords failed: {e}", level="WARNING")
    
    def summary(self):
        """Print summary of changes"""
        print("\n" + "="*70)
        print("DEPLOYMENT SUMMARY")
        print("="*70)
        print(f"✓ Created:   {self.changes['created']} campaigns")
        print(f"✓ Updated:   {self.changes['updated']} campaigns")
        print(f"✗ Errors:    {self.changes['errors']} campaigns")
        print("="*70 + "\n")

# ============================================================================
# MAIN
# ============================================================================

def main():
    """Main deployment workflow"""
    
    parser = argparse.ArgumentParser(
        description="Deploy Treemarkables Google Ads campaigns"
    )
    parser.add_argument(
        "--confirm",
        action="store_true",
        help="Actually deploy changes (default: dry run)"
    )
    args = parser.parse_args()
    
    dry_run = not args.confirm
    
    print("\n" + "="*70)
    print("TREEMARKABLES GOOGLE ADS - CAMPAIGN DEPLOYMENT")
    print("="*70)
    
    if dry_run:
        print("🔍 DRY RUN MODE (no changes will be made)")
        print("   Run with --confirm to actually deploy\n")
    else:
        print("⚠️  DEPLOYMENT MODE (making changes to your account)\n")
    
    # Verify credentials
    verify_credentials()
    
    # Load config
    config = load_config()
    
    # Initialize client
    client = init_google_ads_client()
    
    # Fetch existing campaigns
    manager = CampaignManager(client, CUSTOMER_ID)
    existing = manager.get_existing_campaigns()
    
    print("\n" + "-"*70)
    print("PROCESSING CAMPAIGNS")
    print("-"*70 + "\n")
    
    # Process each campaign
    for campaign_spec in config["campaigns"]:
        campaign_name = campaign_spec["name"]
        daily_budget = campaign_spec.get("daily_budget", 0)
        
        print(f"\n📌 Campaign: {campaign_name} (${daily_budget}/day)")
        
        if campaign_name in existing:
            # Campaign exists - check if budget changed
            existing_budget = existing[campaign_name]["budget_micros"] / 1_000_000
            if abs(existing_budget - daily_budget) > 0.01:
                log(f"  Budget change: ${existing_budget:.2f} → ${daily_budget:.2f}", level="WARNING")
                manager.changes["updated"] += 1
            else:
                log(f"  Already up-to-date", level="SUCCESS")
        else:
            # Create new campaign
            campaign_resource = manager.create_campaign(campaign_spec, dry_run=dry_run)
            
            if campaign_resource:
                # Create ad group
                ad_group_resource = manager.create_ad_group(
                    campaign_resource, campaign_name, dry_run=dry_run
                )
                
                if ad_group_resource:
                    # Create ads
                    manager.create_ads(
                        ad_group_resource,
                        campaign_spec.get("ads", []),
                        dry_run=dry_run
                    )
                    
                    # Create keywords
                    manager.create_keywords(
                        ad_group_resource,
                        campaign_spec.get("keywords", []),
                        dry_run=dry_run
                    )
                    
                    # Create negative keywords
                    manager.create_negative_keywords(
                        ad_group_resource,
                        campaign_spec.get("negative_keywords", []),
                        dry_run=dry_run
                    )
    
    # Print summary
    manager.summary()
    
    if dry_run:
        print("💡 To deploy these changes, run:")
        print("   python3 deploy_campaigns.py --confirm\n")
    else:
        log("✅ Deployment complete!", level="SUCCESS")

if __name__ == "__main__":
    try:
        main()
    except KeyboardInterrupt:
        print("\n\n⛔ Deployment cancelled by user.")
        sys.exit(0)
    except Exception as e:
        log(f"Unexpected error: {e}", level="ERROR")
        import traceback
        traceback.print_exc()
        sys.exit(1)

# Treemarkables Google Ads API - Campaign Deployment Guide

## Overview

This setup uses three files to manage your Google Ads campaigns via the API:

1. **`setup_oauth.py`** — One-time OAuth authorization (generates refresh token)
2. **`campaigns.json`** — Campaign specifications (budgets, keywords, ad copy)
3. **`deploy_campaigns.py`** — Main deployment script (creates/updates campaigns)

**Budget:** $60 NZD/day across 5 campaigns

---

## Quick Start

### Step 1: OAuth Setup (First Time Only)

```bash
python3 setup_oauth.py
```

This will:
- Display a Google authorization URL
- You'll click it, authorize Treemarkables
- You'll be redirected to `localhost:8080` with an auth code
- Copy the code from the URL and paste it when prompted
- The script saves your refresh token to `.env`

**Important:** Add `.env` to `.gitignore` to keep your credentials private.

```bash
echo ".env" >> .gitignore
```

### Step 2: Review Campaign Config

Open `campaigns.json` and verify:
- Campaign names match your strategy
- Daily budgets add up to $60 NZD
- Keywords and ad copy look correct
- Landing page URLs are right

**Current Budget Allocation:**
- Tree Removal: $18/day
- Tree Pruning: $13/day
- Stump Grinding: $9/day
- Hedge Trimming: $12/day
- Emergency/Storm: $8/day

### Step 3: Dry Run (No Changes)

```bash
python3 deploy_campaigns.py
```

This will:
- Read your `.env` credentials
- Load `campaigns.json`
- Show what WOULD be created
- **No changes to your Google Ads account**

Review the output carefully.

### Step 4: Deploy (Make Changes)

Once you're happy with the dry run:

```bash
python3 deploy_campaigns.py --confirm
```

This will:
- Create all 5 campaigns
- Create ad groups within each
- Add keywords with bids
- Add negative keywords
- Deploy responsive search ads

---

## File Breakdown

### `campaigns.json`

Structure:
```json
{
  "account_id": "215-149-0929",
  "currency": "NZD",
  "timezone": "Pacific/Auckland",
  "location_targeting": { ... },
  "campaigns": [
    {
      "name": "Treemarkables - Tree Removal",
      "daily_budget": 18.00,
      "keywords": [ ... ],
      "negative_keywords": [ ... ],
      "ads": [ ... ]
    }
  ]
}
```

**To modify campaigns:**
1. Edit `campaigns.json` (add/remove keywords, change budgets, update ad copy)
2. Run `python3 deploy_campaigns.py` to see what would change
3. Run `python3 deploy_campaigns.py --confirm` to deploy

### `.env` (Auto-Generated)

After running `setup_oauth.py`, this file contains:
```
GOOGLE_ADS_REFRESH_TOKEN=...
GOOGLE_ADS_ACCESS_TOKEN=...
GOOGLE_ADS_CLIENT_ID=...
GOOGLE_ADS_CLIENT_SECRET=...
GOOGLE_ADS_DEVELOPER_TOKEN=...
GOOGLE_ADS_CUSTOMER_ID=215-149-0929
```

**Never commit this to Git.** It contains your credentials.

---

## Campaign Structure

Each campaign has:

**1. Campaign Level**
- Name, daily budget, status
- Geo-targeting: Gisborne, NZ (15km radius)
- Bid strategy: Maximize Conversions
- Ad scheduling with bid adjustments

**2. Ad Group Level**
- Contains all keywords and ads
- Default $2 CPC bid

**3. Keywords**
- Primary keywords (exact match, higher bid)
- Secondary keywords (phrase/broad match, lower bid)
- Bid amounts customized per keyword

**4. Ads**
- Responsive Search Ads (3 headlines, 2 descriptions each)
- All include "work guaranteed" messaging
- Location-specific landing pages

**5. Negative Keywords**
- Blocks DIY, cheap, myself searches
- Prevents wasted spend

---

## Bid Adjustments (Ad Scheduling)

Smart scheduling boosts bids during peak times:

| Campaign | Schedule | Boost |
|----------|----------|-------|
| **Tree Removal** | 7am-5pm weekdays | +20% |
| **Tree Pruning** | 8am-4pm weekdays | +15% |
| **Stump Grinding** | 24/7 (no adjustment) | — |
| **Hedge Trimming** | Weekends (Fri-Sun) | +10% |
| **Emergency** | 5pm-11pm + weekends | +30% |

This ensures your ads show strongest when people are most likely to convert.

---

## Troubleshooting

### "Missing credentials"
→ Run `python3 setup_oauth.py` first to generate `.env`

### "Failed to fetch campaigns"
→ Check that `.env` has a valid refresh token
→ Check developer token is correct

### "Campaign already exists"
→ This is normal. The script checks and skips existing campaigns.
→ To update budgets/keywords, edit `campaigns.json` and re-run with `--confirm`

### "Auth code invalid"
→ Make sure you copied the full code from the localhost URL
→ Don't include `?code=` — just the code itself
→ If issues persist, run `setup_oauth.py` again

---

## After Deployment

### Monitor Performance

Check your Google Ads account:
1. Go to **Campaigns** to see live campaigns
2. Check **Keywords** for impressions and clicks
3. Monitor **Conversion tracking** via GA4 link

### Adjust Bids

Edit `campaigns.json`:
- Increase `bid_amount` for high-performing keywords
- Decrease `bid_amount` for low-performers
- Re-run with `--confirm` to apply

### Add/Remove Keywords

Edit `campaigns.json`:
- Add new keywords to `"keywords"` array
- Remove keywords by deleting from the array
- Re-run with `--confirm` to sync

### Pause/Resume Campaigns

Set `"status": "PAUSED"` in `campaigns.json` and re-run.

---

## API Limits & Costs

- **Google Ads API:** Free tier covers your usage
- **Tokens:** Each deployment uses ~5-10 API calls per campaign
- **Rate limits:** 1,500 requests/day (you're far below this)

**Cost of this automation:** $0 (fully API-based, no UI overhead)

---

## Questions?

- **API docs:** https://developers.google.com/google-ads/api
- **Campaign specs:** Edit `campaigns.json`
- **Dry runs:** Run without `--confirm` to preview changes

Good luck! 🚀

#!/usr/bin/env python3
"""
Google Ads API OAuth 2.0 Setup
One-time script to generate and save refresh token to .env file
"""

import os
import sys
import json
import urllib.request
import urllib.parse
from pathlib import Path

# Your Google Cloud credentials
CLIENT_ID = "819973954312-946ia8864u20476qs6sb00noreq3v248.apps.googleusercontent.com"
CLIENT_SECRET = "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N"
REDIRECT_URI = "http://localhost:8080"
DEVELOPER_TOKEN = "hKuaqYP3FzeXGdSM_p7uMA"

# Google OAuth endpoints
AUTH_URL = "https://accounts.google.com/o/oauth2/v2/auth"
TOKEN_URL = "https://oauth2.googleapis.com/token"

# Google Ads API scope
SCOPE = "https://www.googleapis.com/auth/adwords"

def get_auth_url():
    """Generate the OAuth authorization URL."""
    params = {
        "client_id": CLIENT_ID,
        "redirect_uri": REDIRECT_URI,
        "response_type": "code",
        "scope": SCOPE,
        "access_type": "offline",
        "prompt": "consent"
    }
    return f"{AUTH_URL}?{urllib.parse.urlencode(params)}"

def exchange_code_for_tokens(auth_code):
    """Exchange authorization code for access + refresh tokens."""
    data = {
        "client_id": CLIENT_ID,
        "client_secret": CLIENT_SECRET,
        "code": auth_code,
        "grant_type": "authorization_code",
        "redirect_uri": REDIRECT_URI
    }
    
    req = urllib.request.Request(
        TOKEN_URL,
        data=urllib.parse.urlencode(data).encode('utf-8'),
        headers={'Content-Type': 'application/x-www-form-urlencoded'}
    )
    
    try:
        with urllib.request.urlopen(req) as response:
            return json.loads(response.read().decode('utf-8'))
    except Exception as e:
        print(f"❌ Token exchange failed: {e}")
        sys.exit(1)

def save_to_env(refresh_token, access_token):
    """Save credentials to .env file."""
    env_file = Path(".env")
    
    env_content = f"""GOOGLE_ADS_REFRESH_TOKEN={refresh_token}
GOOGLE_ADS_ACCESS_TOKEN={access_token}
GOOGLE_ADS_CLIENT_ID={CLIENT_ID}
GOOGLE_ADS_CLIENT_SECRET={CLIENT_SECRET}
GOOGLE_ADS_DEVELOPER_TOKEN={DEVELOPER_TOKEN}
GOOGLE_ADS_CUSTOMER_ID=215-149-0929
"""
    
    env_file.write_text(env_content)
    print(f"\n✅ Credentials saved to .env")
    print(f"📝 Refresh token: {refresh_token[:20]}...")
    print("\n⚠️  IMPORTANT: Add .env to .gitignore to keep credentials private!")

def main():
    print("=" * 70)
    print("🔐 Google Ads API OAuth 2.0 Setup - Treemarkables")
    print("=" * 70)
    
    # Step 1: Generate auth URL
    auth_url = get_auth_url()
    print(f"\n1️⃣  Visit this URL to authorize Treemarkables:")
    print(f"\n{auth_url}\n")
    print("2️⃣  You'll be redirected to localhost with an auth code")
    print("3️⃣  Copy the code from the URL and paste it below\n")
    
    # Step 2: Get auth code from user
    auth_code = input("Paste auth code here: ").strip()
    
    if not auth_code:
        print("❌ No auth code provided. Exiting.")
        sys.exit(1)
    
    print(f"\n⏳ Exchanging code for tokens...")
    
    # Step 3: Exchange code for tokens
    token_response = exchange_code_for_tokens(auth_code)
    
    if "refresh_token" not in token_response:
        print(f"❌ Failed to get refresh token: {token_response}")
        sys.exit(1)
    
    refresh_token = token_response["refresh_token"]
    access_token = token_response["access_token"]
    
    # Step 4: Save to .env
    save_to_env(refresh_token, access_token)
    
    print("\n" + "=" * 70)
    print("✅ OAuth Setup Complete!")
    print("=" * 70)
    print("\nNext steps:")
    print("1. Run: python3 deploy_campaigns.py")
    print("2. Review the dry run output")
    print("3. Run: python3 deploy_campaigns.py --confirm")
    print("\nYou're all set! 🚀\n")

if __name__ == "__main__":
    main()
