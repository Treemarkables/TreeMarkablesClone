# Work Registry — collision prevention for parallel sessions

Many Claude Code sessions run at once in separate git worktrees against this repo.
**They cannot see each other.** This has caused duplicate work (two complete billing
systems were built in parallel before anyone noticed). Claim substantial feature
work here so it doesn't happen again. See the "Avoiding duplicate work" rule in
`CLAUDE.md`.

## How to use
1. **Before** starting a feature: `git fetch origin main && git log origin/main --oneline -25`,
   `gh pr list`, and scan the **Active** claims below for overlap.
2. If your feature already exists on `main` or in an open PR/claim, build the *delta* —
   don't create a parallel copy. Reconcile with that branch instead.
3. If clear, add a claim under **Active** (newest at top).
4. When your PR merges (or you abandon the branch), move the line to **Recently done**
   or delete it. Prune **Recently done** periodically.

Format: `- <area> — branch \`<name>\` — <YYYY-MM-DD> — <one-line scope> [PR #<n>]`

## Active
- Inbound-email generic fallback → tenant scoping — branch `feat/inbound-email-tenant-scoping` (off main) — 2026-06-28 — the LIVE Gmail poller `server/services/gmailReplyService.ts` generic fallback (STEP 2 find-customer-by-from-email, STEP 3 brand-new-lead conversation) ran on the owner connection across ALL tenants → could match another business's customer/conversation. NOW resolves the email's To address via `resolveBusinessIdByChannel('email', email.to)` (To header survives Cloudflare→Gmail forward = the per-tenant registered address) and self-scopes: STEP 2 customer+job lookups get explicit `businessId` filters; STEP 3 wrapped in `runWithBusiness(channelBusinessId)` (withTenant stamps creates) + explicit guards on the two cross-tenant reads (findExistingOpenConversation result + getAllConversations loop). STRICTLY ADDITIVE: undefined when To isn't a registered channel → unchanged single-tenant behaviour (TM's generic inbound usually unmapped → no regression). NOTE on mechanism: `runWithBusiness` alone does NOT scope reads (no pinned tenantDb; `db` proxy → ownerDb) — only `withTenant` write-stamping — hence explicit read filters/guards. Job-addressed paths already resolve via jobId (unchanged). SMS poller NOT touched (shared SMS Everyone number = no per-tenant signal; needs per-tenant numbers, product decision). esbuild clean. No schema. Builds on PR #234 channelMap + complements #279 registration UI.
- Inbound channel registration UI/API — branch `feat/inbound-channel-registration` (off main) — 2026-06-28 — MERGED [PR #279] — replaces the manual SQL insert at onboarding for `tenant_channels` (the phone/email→businessId map from PR #234 / `server/tenancy/channelMap.ts`). NEW REST endpoints in `routes.ts` (requireAdmin, scoped to `req.session.businessId`): `GET/POST /api/channels` + `DELETE /api/channels/:id`. POST normalizes via `normalizeChannelIdentifier`, pre-checks the GLOBAL unique (channel_type,identifier) via `resolveBusinessIdByChannel` → 409 if owned by another tenant, idempotent + reactivates an own inactive row. Only `phone`+`email` offered (NOT fb_page — Facebook dropped). NEW page `client/src/pages/SettingsChannels.tsx` (list + add form, Shadcn Select/Card, destructive-only toasts) + route `/settings/channels` in `App.tsx` + a "Inbound Channels" card in `Settings.tsx` business section. No schema change (table + RLS policy already on main). esbuild + vite clean. DEFERRED: cross-tenant/platform-admin management (this is per-logged-in-tenant; Jules registers via concierge login); SMS-poller + Messenger consumers still use default-tenant fallback per [[project_tenant_channel_map]] (inbound-email consumer wired in PR #281).
- Seed per-tenant default PDF/document templates at signup — branch `feat/seed-tenant-doc-templates` (off main) — 2026-06-28 — `createTenant()` (`server/onboarding.ts`) created business+settings+admin+Freemium but NO `document_templates`, so a new tenant's session-less proposal/quote/invoice viewers fell back to an arbitrary (Treemarkables) default = cross-tenant branding leak (relates to tenant isolation audit; complements `getDefaultDocumentTemplateForBusiness` #239). NOW seeds 3 rows (quote/proposal/invoice, isDefault+isActive, scoped to biz.id) with company identity set EXPLICITLY (companyName=businessName, companyEmail=admin email, address/phone/GST = '' blank) — because the column defaults are TM's (companyName 'Treemarkables LTD', GST '131-047-592…'), an unset insert would re-leak. Added to compensating rollback too. esbuild clean. No schema change (uses existing table). DEFERRED: existing template-less tenants (e.g. the demo tenant) still fall back — needs a one-off backfill if desired; address/phone blank until owner fills Company Info.
- Forward customer job replies to subscriber inbox — branch `feat/job-reply-forwarding` (off main) — 2026-06-28 — MERGED [PR #274] — optional per-tenant "copy customer replies to my own email" (subscribers currently only get job replies in-app via diary+push; only TM gets a Gmail copy via Cloudflare→accounts@treemarkables.nz). NEW nullable `business_settings.job_reply_forward_email` (`shared/schema.ts` + self-healing boot DDL `ALTER TABLE … ADD COLUMN IF NOT EXISTS` in `server/index.ts`; blank/null = OFF = current behaviour, so TM/existing tenants unaffected). When set, a COPY of each inbound customer reply is emailed there — From name = customer, Reply-To = customer (owner answers directly), body = the reply; reply still logged on the job card regardless. Loop-guarded (skip if forwardTo == sender). Wired in BOTH inbound paths: `/api/webhooks/email` (3 match branches: UUID/job-number/quote) via a `forwardReplyToOwnerInbox(job)` closure, AND the LIVE Gmail poller `server/services/gmailReplyService.ts` (Cloudflare→Gmail→poll is the real prod path). Settings UI field in `client/src/pages/SettingsCompany.tsx` ("Forward customer replies to"). Builds on #272's `emailService.sendEmail({ fromName })`. esbuild + vite clean. TODO: column auto-applies at boot on deploy (no manual migration needed, but confirm on prod). DEFERRED: owner's reply from their inbox goes direct to customer, NOT re-logged on the job card (matches how TM's Gmail works today).
- Per-tenant email SENDER identity (From name) — branch `feat/per-tenant-email-sender` (off main) — 2026-06-28 — MERGED [PR #272] — the delta on top of `feat/email-branding-per-tenant` (#?, body wordmark/GST): outbound `from:` was hardcoded `Treemarkables <info@updates.treemarkables.co.nz>` for every tenant. NEW `emailService.sendEmail({ fromName })` composes the tenant's business name over the **shared verified sending address** (`composeFrom`/`extractEmailAddress` in `server/services/emailService.ts`) — zero per-subscriber DNS/setup, name strips header-injection chars + quotes commas, blank → platform default (TM byte-identical). Wired the 3 customer-facing job-card sends (proposal/quote/invoice in `routes.ts`) to pass `fromName: __emailIdentity.name` (same `getBusinessIdentity()` the body already uses). Reply-to UNCHANGED (still job-scoped → preserves job-card auto-filing + jobId→tenant). ALSO fixed two latent bugs in legacy `notificationService`-called methods: `sendQuoteEmail` referenced an undefined `businessName` (runtime ReferenceError) + both `sendJobStatusEmail`/`sendQuoteEmail` set `from: this.fromEmail` (undefined property) → now `fromName: businessName`. esbuild clean. No schema/migration. DEFERRED: legacy notification-email BODY copy still tree-specific (🌳/"tree services"/localhost links); reply-to job-NUMBER collision across tenants (inbound concern, pre-existing); per-tenant sending DOMAIN (currently shared TM domain — one-line swap when an Inflow domain is verified).
- Stripe webhook payment-branch tenant binding — branch `fix/stripe-webhook-tenant-binding` (off main) — 2026-06-27 — `/api/stripe/webhook` (`routes.ts` ~25024/25059): the `kind='payment'` INVOICE branch (createPayment + updateInvoice) and JOB branch (createPayment + updateJob) ran session-less with NO `runWithBusiness`, so the payment-ledger rows + invoice/job updates stamped the DEFAULT tenant (Treemarkables) — the proposal/deposit branch right below already binds correctly. Wrapped both in `runWithBusiness(invoice.businessId / job.businessId)`, mirroring the proposal branch. Lookup is by trusted Stripe metadata (invoiceId/jobId) so it could only mis-stamp to default, not cross to an attacker-chosen tenant. esbuild clean. No schema/migration.
- RLS direct-endpoint boot guard — branch `feat/rls-direct-endpoint-guard` (off main) — 2026-06-27 — closes RLS-mechanism audit gap: `assertTenantDbMatchesOwner` only compares row counts, which PASS for a same-DB pooler URL, so it can't catch a `DIRECT_DATABASE_URL` accidentally pointing at a POOLER endpoint → session GUCs cross-contaminate between tenants under load (cross-tenant reads). Added an explicit check in `server/db.ts` `assertTenantDbMatchesOwner`: throws (fail-closed, wired to process.exit at boot) if `DIRECT_URL` contains `-pooler.` or `pgbouncer=true`, password masked in the error. Only a misconfigured DIRECT_DATABASE_URL trips it (auto-derivation strips `-pooler.`). esbuild clean; logic unit-checked. No schema/migration. [PR #245]
- Company Info identity + bank Settings UI — branch `feat/bank-details-settings-ui` (off main) — 2026-06-27 — extends `client/src/pages/SettingsCompany.tsx` with an "Emails & AI" section (brand name / owner / tagline / trade → business_settings) + a "Bank details" section (bankAccountName / bankAccountNumber → business_settings), via the existing `PUT /api/business-settings` dual-write (GST synced to both stores). MERGED [PR #242].
- Per-tenant invoice bank details (trade-gen Phase A) — branch `feat/per-tenant-invoice-bank` (stacked on `feat/public-proposal-tenant-identity`/#239) — 2026-06-27 — fixes the WORST leak: public invoice (`InvoiceView.tsx`) hardcoded TM's BANK ACCOUNT for every tenant → misdirected payments. NEW `business_settings.bankAccountName`/`bankAccountNumber` (migration `20260627_business_bank_details.sql` + isolated split-statement boot DDL + TM seeded by name) + `storage.getBusinessSettingsForBusiness(businessId)`. `/api/invoices/:id/public` now returns a `company` object (contact from doc template, bank from settings, scoped to invoice.businessId); InvoiceView renders the payment block ONLY when bank set (else hidden — never TM's), + de-hardcoded footer/contact/logo-alt. Verified on dev: TM seeded+unchanged, real tenant "Cut right" shows no bank block. esbuild+vite clean. TODO: run migration dev✓→prod; DEFERRED: Settings UI for tenants to enter their bank (Company Info payment section).
- Public document viewer de-hardcoding (trade-gen Phase A) — branch `feat/public-proposal-tenant-identity` (off main) — 2026-06-27 — public/session-less doc routes leak Treemarkables branding (unscoped `getDefaultDocumentTemplate` returns TM's template for every tenant's customer). NEW `storage.getDefaultDocumentTemplateForBusiness(businessId, type)` (ownerDb, scoped by the DOCUMENT's businessId, unscoped fallback for TM legacy null-business_id rows). DONE: proposal accept (`/api/proposals/:id/public` scoped to `proposal.businessId` + server/client TM-literal fallbacks → blank; `ProposalAccept.tsx`). Verified on dev: TM proposal template carries its business_id → unchanged; configured tenants get their own. esbuild + vite clean. **DEFERRED + ⚠️ URGENT: invoice view (`InvoiceView.tsx`/`/api/invoices/:id/public`) hardcodes TM's BANK ACCOUNT (`Treemarkables Ltd`/`06-0637-0768850-00`) — every tenant's invoice misdirects customer payments to TM. NO per-tenant bank field exists; needs a product decision (bank fields vs Stripe pay-link) + schema + UI. Its own PR.** Also deferred: authenticated previews (ProposalViewer/QuoteViewer/InvoiceViewer/CustomerPortal — logged-in, lower risk).
- Per-tenant email branding (trade-gen Phase A follow-up) — branch `feat/email-branding-per-tenant` — 2026-06-27 — kill the hardcoded `TREEMARKABLES`/GST leak in branded customer emails. `server/emailTemplates.ts`: header wordmark + subtitle were hardcoded (ignored `opts.company`); now per-tenant (wordmark = business name uppercased via CSS so TM stays "TREEMARKABLES" byte-identical; subtitle = businessTagline). GST footer now renders ONLY when set — `getBusinessIdentity().gstNumber` defaults to `''` with **no TM fallback** (the leak). NEW `businessSettings.businessGstNumber` column (`shared/schema.ts` + `migrations/manual/20260627_business_gst_number.sql` + isolated split-statement boot DDL in `server/index.ts`), TM's real GST seeded by name (works on both branches). 3 `renderBrandedEmail` callers pass tagline+gstNumber. Render-tested: TM unchanged + GST present; a plumber tenant leaks zero "Treemarkables"/"arborist"/TM-GST. esbuild clean; dev migration applied + TM seeded. ⚠️ ONE intentional TM-visible change: email header subtitle "Professional Arborists · Gisborne" → TM's own tagline "Qualified Arborists" (consistent w/ its PDF footer). TODO: run migration dev✓→prod; DEFERRED: PDF GST (`document_templates.gstNumber` path routes.ts:1334/1543) + business-identity Settings UI (ownerName/tagline/discipline/GST all lack a form). Plan: `INFLOW_TRADE_GENERALIZATION_PLAN.md` line 219.
- Inbound channel→tenant map (Group B infra) — branch `claude/heuristic-lovelace-d9ebc4` — 2026-06-27 — the phone#/email/FB-page → businessId map the deferred Group B paths need. NEW global table `tenant_channels` (`shared/schema.ts` + `migrations/manual/20260627_tenant_channels.sql`, RLS ENABLED-not-FORCE + tenant_isolation policy + guarded app_tenant grant + idempotent boot DDL in `server/index.ts`). NEW `server/tenancy/channelMap.ts` (`resolveBusinessIdByChannel` on ownerDb, `normalizeChannelIdentifier` [phone=last-8, matches findCustomerByPhoneLast8], `upsertTenantChannel`, `seedChannelsFromEnv`). Boot seeds the existing tenant's channels from ENV (TWILIO/OWNER/HERO phone + business_email) so single-tenant resolution is unchanged. WIRED 1st consumer: `/api/webhooks/twilio-voice` resolves the dialed line up-front → stamps createCall + scopes the cross-tenant caller match + createCustomer to the resolved tenant (fall-through-safe: unmapped number = prior behaviour). esbuild clean. TODO before deploy: run the migration dev→prod. DEFERRED follow-ups (need per-tenant channel registration UI / product decisions): SMS poller match-within-tenant, inbound-email→business, Messenger (FB-page→business), `/api/webhooks/contact-form` + `/api/webhooks/messenger` (no signal); admin UI to register a tenant's numbers/emails (today = SQL insert at onboarding). Distinct from PR #201 (tenant-aware admin push). PART TWO (same branch/PR): RLS-policy backstop — fixed `equipment_compliance_reminders` (had business_id, no policy → cross-tenant readable under the blanket app_tenant GRANT) via boot DDL + `migrations/manual/20260627_equipment_compliance_reminders_rls.sql`; NEW generalized guard `assertTenantTablesHaveRlsPolicies` / `findTenantTablesMissingRlsPolicy` in `server/db.ts` (boot: logs loud, hard-fails only under `TENANT_RLS_STRICT=true`; exempt via `TENANT_RLS_POLICY_EXEMPT`) + CLI gate `scripts/checkTenantRlsPolicies.ts` (always non-zero on a gap — for CI/pre-deploy). **The guard flagged a 2nd offender: `usage_counters` (rls off, no policy) — belongs to draft PR #140 (`inflow-app-platform-clean`), NOT on main/prod. Whoever lands #140 MUST add ENABLE RLS + tenant_isolation policy to usage_counters or the CI gate / boot guard will flag it.**
- SMS/AI usage-cap enforcement — branch `claude/cranky-bhabha-3c42de` — 2026-06-22 — Phase A+B BUILT (branch, undeployed): schema (`usage_events` table + `sms_cap`/`ai_action_cap` on subscription_plans + `overage_policy` on subscriptions in `shared/schema.ts`) + migration `migrations/manual/20260622_usage_caps.sql` (NOT yet run); `server/services/usageMeter.ts` (checkAllowance/recordUsage/guard/getUsageSummary, NZ-month counting, comped-allowlist, fail-open); SMS choke-point wired in `smsService.sendSMS` (guard+record); `GET /api/billing/usage`. Dark-launched: recording always-on, BLOCKING only when `USAGE_CAPS_ENFORCE=true`. Pre-migration = fails open (no metering). esbuild clean. Phase C DONE: AI guards on all 10 AI endpoints in routes.ts (guard before OpenAI/Whisper, record after success, 429 on block) + cron SMS threaded (bookingReminderService passes job.businessId) + quote-followup AI metered in reminderChecker (quote.businessId). Note: notificationService SMS left to ALS (request-context). Phase D DONE: SettingsBilling usage-meter card (GET /api/billing/usage) + corrected plan feature copy (removed marketing, AI/analytics now on both tiers). Full server bundle clean. TODO: run migration (dev→prod via Neon), then deploy; flip USAGE_CAPS_ENFORCE=true after watching usage data; Phase E = metered overage (Stripe usage records) still deferred. Also revised pricing to Crew $85/Business $130 (live Stripe prices swapped 2026-06-22) + marketing suite removed/AI+analytics on both paid tiers/Facebook dropped. SCOPED (`INFLOW_USAGE_CAPS_PLAN.md`). Enforce the bundled SMS (200/600) + AI-action (75/250) monthly allowances that are currently unenforced (subscription_plans only has active_job_cap). Plan: new `usage_events` table + `sms_cap`/`ai_action_cap` columns + `overage_policy`; `server/services/usageMeter.ts`; soft-stop at the single SMS choke point (`smsService.sendSMS`) + the ~11 AI endpoints; calendar-month NZ period; comped TM = unlimited; metered overage deferred. Also revised pricing in `INFLOW_SAAS_PLAN.md` (Crew $85 / Business $130, marketing suite removed, AI+analytics on both paid tiers, Facebook integration dropped) + `scripts/createInflowStripePrices.mjs` to mint the new $85/$130 Stripe prices. Distinct from `entitlement enforcement`/PR #132 (that gates feature on/off by tier; this gates *quantity*).
- App Store readiness (B2B web-billing path) — branch `feat/appstore-readiness-and-tenancy-isolation-test` — 2026-06-20 — make the iOS shell App-Store-submittable without Apple IAP. DONE: (1) gate all subscription checkout/portal CTAs in `SettingsBilling.tsx` behind `!isNativeApp()` (new `client/src/lib/platform.ts`), show read-only plans + "manage at inflowapp.co.nz" on native; (2) in-app account deletion (Guideline 5.1.1(v)) — `DELETE /api/auth/account` (scrub PII + sever login via `storage.anonymizeEmployeeForDeletion`, no hard-delete b/c 20+ unguarded FKs to employees.id; drops fcm tokens; destroys session) + new `SettingsAccount.tsx` page (`/settings/account`, typed-DELETE confirm) + "Account" card on Settings landing. Vite + server esbuild both clean. (3) cross-tenant isolation test harness `scripts/tenancyIsolationTest.mts` — seeds two throwaway tenants, logs in as each admin over HTTP, drives a list/direct-id/portal endpoint matrix and asserts no cross-tenant id leak (UUID-substring scan); prints PASS/SECURITY-FAIL/WARN matrix, exit code = #leaks. Needs dev server up w/ TENANT_RLS_ENABLED=true. NOT done (mostly non-code / your side): App Store Connect listing/screenshots/privacy-labels/reviewer demo account, optional capacitor `server.url` → Inflow domain; closing the portal/public-link/webhook owner-path gaps before tenant #2.
- compliance expiry reminders — branch `feat/compliance-expiry-reminders` — 2026-06-15 — Phase 2 "alerts" for the /today fleet compliance: daily cron scan (server/services/complianceReminderService.ts, hooked into the 60s notification worker, self-throttled hourly) fires in-app notification + push to admins when a vehicle's rego/CoF/scheduled-service crosses a configured lead time (and once overdue). Configurable lead times (default [30,7] days) on business_settings (`compliance_reminders_enabled` + `compliance_reminder_offsets`); settings UI = ExpiryReminderSettings card on /settings/vehicle-inspections. Dedupe via new `equipment_compliance_reminders` table (unique equipment_id+kind+expiry_date+offset_days). Boot DDL in server/index.ts. Builds on the merged /today work below.
- today / daily command centre — branch `claude/jolly-curie-0fc41e` — 2026-06-15 — new `/today` page (TodayDashboard.tsx) + `GET /api/today-overview` aggregator: fleet compliance (rego/CoF/scheduled-service incl. OVERDUE — note `/api/equipment/expiring` only covers next-30-days, skips overdue + service) + today's NZ-calendar jobs + summary tiles. Sidebar "Today" link above Dispatch. Phase 1 = page (read-only, reuses existing storage, no schema/migration). Phase 2 (planned) = morning cron scanning expiry dates → notifications + push (the "alerts" half)
- staff-schedule multi-week views — branch `feat/staff-schedule-multiweek` — 2026-06-12 — 1/2/3/4-week view toggle on /staff-schedule: scrollable readable availability grid (staff × days, job chips w/ names+times, heatmap tints, unassigned lane, per-day revenue footer), new `/api/jobs/for-date-range` endpoint, drill-to-day; Day Gantt unchanged [PR #176]
- inbound caller-ID wrong name — branch `fix/caller-id-forwarded-from` — 2026-06-12 — forwarded calls identified the caller as the owner's own number (ForwardedFrom preferred over From, semantics inverted) so caller-ID matched whatever customer record held that number ("Kim"); flip to From-first at all three webhook sites; findCustomerByPhoneLast8 scoped by currentBusinessId() when set + deterministic ORDER BY
- call-ring diagnosability — branch `fix/call-ring-diagnosability` — 2026-06-11 — voicemail recordings no longer logged as status 'answered' (now 'missed' via the existing &source=voicemail param); /api/twilio/admin/diagnostic recentCalls reads the right table (calls, not call_records); iOS app surfaces Twilio voice registration failures as a destructive toast instead of silent console logs
- quote online view/accept + PDF description — branch `fix/quote-view-accept-online` — 2026-06-11 — quote email CTA becomes "View Quote" → quote-aware ProposalAccept page (Accept Quote → finalizeProposalAcceptance, Download PDF, /viewed ping); PDF renders section descriptions + clickable accept-online link; email-reply fallback kept [PR #168]
- multi-day diary day-picker — branch `claude/nifty-ritchie-4b2bad` — 2026-06-09 — schedule a job across an arbitrary set of days (calendar multi-select in the Schedule Job modal + "remove weekends" toggle) instead of a contiguous start/end range. Adds `jobs.scheduled_dates` jsonb + `getJobScheduledNZDates`/`jobRunsOnNZDate` helpers; staff-assignment expansion + CalendarGrid + DispatchBoard honour the day set. Migration `migrations/manual/20260609_job_scheduled_dates.sql` (run on dev + prod before deploy)
- RLS session-less paths — branch `claude/festive-rubin-2cef4b` — 2026-06-08 — fix RLS prod regression: allowlist public viewers + webhooks + review flow to owner connection in `tenantMiddleware.ts` so they don't fail closed under `TENANT_RLS_ENABLED`. Also surfaces the real error in proposal/quote send-email routes. Immediate mitigation = unset the flag in DO.
- video auto-captions — branch `claude/romantic-blackburn-e8634c` — 2026-06-07 — Loom-style WebVTT captions on walkthrough videos (Whisper segment timestamps, auto on upload, `<track>` on all players). videos.captions_* columns applied to dev + prod Neon branches
- entitlement enforcement — branch `inflow-entitlement-enforcement` — 2026-06-06 — gate RBAC features by subscription tier, flag-gated [PR #132]
- work registry / collision-prevention — branch `inflow-work-registry` — 2026-06-06 — this file + CLAUDE.md rule
- Onboarding setup checklist — branch `feat/onboarding-checklist` (off main) — 2026-06-28 — (appended at BOTTOM of Active intentionally — top-insert collisions on this file were causing repeated PR merge-conflicts; bottom-append avoids them.) NEW `GET /api/onboarding/checklist` (requireAdmin, `req.session.businessId`) aggregates bring-your-own setup fields into a progress list: businessName/ownerName (business_settings), logo/contact/address (default invoice document_template), bank/GST/tradeVocabulary/jobReplyForwardEmail (business_settings), inbound channels (tenant_channels count); 7 required + 3 optional, returns items[]+requiredDone/requiredTotal. NEW page `client/src/pages/SettingsSetup.tsx` (/settings/setup): progress bar + per-item done/Set-up links. "Finish Setup" card added FIRST in Settings.tsx business section (Rocket icon). Read-only; each item deep-links to /settings/company or /settings/channels. esbuild + vite-transform clean. No schema. DEFERRED: live progress badge on the landing card + a dashboard banner (page-only for now). MERGED [PR #283].
- Onboarding dashboard banner — branch `feat/onboarding-dashboard-banner` (off main) — 2026-06-28 — the deferred dashboard-banner follow-up to #283. NEW `client/src/components/OnboardingBanner.tsx`: slim setup-progress strip reading `GET /api/onboarding/checklist` ("Finish setting up — X of N essentials done" + Continue-setup link to /settings/setup + dismiss). Auto-hides when complete (requiredDone>=requiredTotal), dismissed (localStorage `onboarding_banner_dismissed`), or unreadable (non-admin 401 → retry:false → null). Rendered above `<DispatchBoard/>` in `Dispatch.tsx` (the default landing /dispatch; shrink-0 strip). No backend/schema change (reuses #283 endpoint). esbuild clean.
- Concierge admin view (cross-tenant subscriber management) — branch `feat/concierge-admin-view` (off main) — 2026-06-28 — MERGED [PR #286] — platform-operator can set up/inspect ANY subscriber during onboarding without logging in as them. NEW gate `requirePlatformAdmin` (requireAdmin + `req.session.businessId ∈ TREEMARKABLES_BUSINESS_IDS`). Extracted shared `buildOnboardingChecklist(businessId)` helper (refactored the #283 endpoint onto it). NEW endpoints (all requirePlatformAdmin, cross-tenant via ownerDb storage helpers): `GET /api/admin/subscribers` (list + per-biz progress), `GET /api/admin/subscribers/:id` (business+settings+channels+checklist), `PUT /api/admin/subscribers/:id/settings` (whitelisted Company-Info business_settings fields only). NEW page `client/src/pages/AdminSubscribers.tsx` (/admin/subscribers). "Subscribers" card in an operator-only "Platform" section of Settings.tsx, gated via `useRoleChecklistFeature()`. (channel ADD/DELETE → PR #288; plan/status → PR #291.)
- Concierge cross-tenant channel management — branch `feat/concierge-channel-mgmt` (off main) — 2026-06-28 — [PR #288] — follow-up to #286: operator can register/remove a subscriber's inbound channels on their behalf. NEW endpoints (requirePlatformAdmin, cross-tenant): `POST /api/admin/subscribers/:id/channels` (mirrors per-tenant POST: normalize + global-unique 409 via resolveBusinessIdByChannel + idempotent/reactivate, but targets :id) + `DELETE /api/admin/subscribers/:id/channels/:channelId`. NEW ownerDb storage: `findTenantChannelForBusiness`, `insertTenantChannel`, `setTenantChannelActive`, `deleteTenantChannelForBusiness`. `AdminSubscribers.tsx` channels card now editable. Merged main 2026-06-28 (kept both #288 channel + #291 plan code in AdminSubscribers.tsx + storage.ts). esbuild+vite clean. No schema.
- Concierge plan & status management — branch `feat/concierge-plan-management` (off main) — 2026-06-28 — MERGED [PR #291] — adds suspend + manual-plan comping to the concierge view. NEW endpoints (requirePlatformAdmin, ownerDb): `PUT /api/admin/subscribers/:id/status` (active|suspended; refuses suspending an operator business) + `PUT /api/admin/subscribers/:id/plan` (sets subscription.planId for MANUAL subs only — 409 if `stripeSubscriptionId` set, to avoid Stripe-webhook drift). Detail endpoint now also returns `subscription` {status,planId,planKey,planName,stripeManaged} + `plans` catalog. NEW ownerDb storage: `getBusinessById`, `setBusinessStatus`, `getSubscriptionForBusiness`, `listSubscriptionPlans`, `setSubscriptionPlanForBusiness`. ENFORCEMENT: `/api/auth/login` now blocks login when `businesses.status==='suspended'` (operator/TREEMARKABLES exempt → no self-lockout; existing sessions persist till expiry). UI: "Plan & status" card in `AdminSubscribers.tsx`.
- Concierge hardening (post security-review) — branch `feat/concierge-hardening` (off main) — 2026-06-28 — two low-severity hardening fixes from the security review of #286/#288/#291: (1) `requirePlatformAdmin` now allowlist-checks `employee.businessId` (authoritative DB value) instead of `session.businessId` (defence-in-depth vs stale session); (2) `[CONCIERGE_AUDIT]` console logs on all 5 cross-tenant mutations (set_status/set_plan/update_settings/add_channel/remove_channel) recording operator employeeId + target businessId + action — grep DO logs for `CONCIERGE_AUDIT`. Review verdict was no critical/high findings. routes.ts only. esbuild clean. No schema. NOT fixed (cosmetic/feature, noted): channel-add 409 misses inactive-other-tenant rows → 500 edge. ENTITLEMENTS-COMP VERIFIED 2026-06-28: comped plans DO grant features (resolveEntitlements + usageMeter + /me all read subscription.planId+status; no Stripe dep); server enforcement dark-launched behind FEATURE_GATES_ENFORCE.
- Document-template backfill for pre-#276 tenants — branch `feat/backfill-doc-templates` (off main) — 2026-06-28 — closes the residual leak #276 left for EXISTING template-less tenants (e.g. the demo tenant): their public proposal/quote/invoice views fell back to the unscoped (Treemarkables) default template. NEW `backfillMissingDocumentTemplates()` in `server/onboarding.ts` (ownerDb, idempotent): for each business with NO `document_templates`, seed the 3 defaults (quote/proposal/invoice, isDefault+isActive) scoped to it, identity from its own business_settings (companyName/email/phone/address/gst) — blank where unset, NEVER TM's column defaults. Businesses with templates (incl. TM) skipped. Extracted shared `DEFAULT_DOC_TEMPLATES` const (reused by createTenant). Wired as a self-healing boot step in `server/index.ts` (after the document-template-defaults migration; runs every deploy, cheap after first). esbuild clean. No schema.
- Android + Electron desktop apps (full parity) — branch `feat/android-electron-apps` (off main) — 2026-06-29 — porting the existing remote-URL Capacitor iOS shell to Android (FULL parity: FCM push + Twilio Voice inbound calls via ConnectionService) and adding an Electron desktop shell. Mirrors the iOS `ios-native/` staging pattern: NEW `android-native/` (Kotlin plugin + FCM service + ConnectionService + gradle/manifest additions), `scripts/android-setup.sh` + `scripts/android-sync.sh`, NEW standalone `electron/` (own package.json, loads app.treemarkables.co.nz). SERVER: `/api/twilio/token` now platform-aware — returns the Android FCM push-credential SID (`TWILIO_PUSH_CREDENTIAL_SID_ANDROID`) when the client posts `{platform:'android'}`, else the iOS APNs one. Docs: `ANDROID_BUILD_GUIDE.md` + `DESKTOP_BUILD_GUIDE.md`. NOTE: this machine has no Android SDK/Electron deps — native build+device test happens on the owner's Android Studio install (same as iOS→TestFlight). NEW env (DO): `TWILIO_PUSH_CREDENTIAL_SID_ANDROID`. Desktop has NO push/calls (no mobile SDK on desktop) — documented.
- Widen plan feature-gates to remaining premium modules — branch `feat/widen-plan-gates` (off main) — 2026-06-29 — owner confirmed FEATURE_GATES_ENFORCE + USAGE_CAPS_ENFORCE are BOTH ON in prod (enforcement LIVE), and asked to close the gap between UI gating (PlanGate, always on) and API gating (requireEntitlement, only ~6 route groups). Added prefix gates for the ENTIRELY-premium modules only: Crew → `/api/equipment`, `/api/job-templates`, `/api/follow-up-queue`, `/api/booking-reminders`, + safety/equipment sub-resources `/api/safety-assets`/`/api/swms-templates`/`/api/toolbox-talk-topics`/`/api/toolbox-talk-attendees`/`/api/induction-photos`; Business → `/api/campaigns`. DELIBERATELY NOT blanket-gated (mixed free+paid prefixes — would break freemium): /api/staff (staff.view free), /api/photos, /api/jobs+dispatch, /api/videos (public/knowledge), /api/documents+document-templates (free invoice/proposal PDFs), /api/analytics (basic free), /api/materials+/api/services (read by free quoting), /api/conversations+/api/reviews+/api/price-rules (entangled w/ free lead/quote), /api/servicem8 (left open for onboarding import) — these stay UI-gated until per-HANDLER gating lands. TM + session-less always exempt. esbuild clean. No schema. RESIDUAL GAP (noted): mixed-prefix capabilities (dispatch.manage, photos.captions/timeline, staff.manage, documents.build, analytics.advanced, materials.manage, inbox, reviews/marketing-business subset) need handler-level requireCapability, a separate careful pass.

## Recently done
- Tenant resolution Group A (owner-context public WRITE paths: quote/proposal/review accept + Stripe deposit webhook stamp the resource's tenant, not DEFAULT) — merged to `main` 2026-06-20 [PR #196]. **GROUP B still pending before tenant #2** (corrected scope per owner 2026-06-20): Twilio call-recording webhook `/api/webhooks/twilio-voice` (Twilio = recording only); SMS Everyone inbound reply POLLER `server/services/smsReplyPoller.ts` (SMS is SMS Everyone, NOT Twilio — replies are polled, not webhooked; `/api/webhooks/sms` Twilio route is legacy/unused); `/api/webhooks/resend-events` + inbound `/api/webhooks/email`; `/api/webhooks/messenger` + `/api/webhooks/contact-form` (NO tenant signal → product decision: per-business form URLs / FB-page mapping). **Drop Vonage** (`vonage-*`) — unused, dead-code removal candidate, no tenant work. Needs phone#→business + email→business mapping infra. Harmless while single-tenant.
- Phase 4 billing (Stripe checkout + webhook sync + UI + self-serve signup) — merged to `main` 2026-06
- Trade generalization plan (docs) — merged to `main` 2026-06 [PR #124]
- Phase 1 multi-tenancy (`business_id` on all tables) — on `main` / prod

## Known stale branches (candidates to close)
- `feat/roles-permissions` [PR #11] — its RBAC backend is already identical on `main`; superseded.
- `inflow-phase4-billing-foundation` — deleted 2026-06-06 (was the duplicate billing PR #126).
