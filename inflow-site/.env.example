# Endpoint that the "Request access" form POSTs JSON to.
# If unset, the form falls back to a mailto: link to hello@inflowapp.co.nz.
# VITE_REQUEST_ACCESS_ENDPOINT=https://api.example.com/inflow-access
