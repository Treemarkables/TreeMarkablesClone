# Where Sign up and the contact form send people.
# Defaults to https://app.inflowapp.co.nz/signup, which creates a tenant via POST /api/signup.
# Set this only to point a non-production build at another app. Do not use a mailto: link.
# VITE_APP_SIGNUP_URL=https://app.inflowapp.co.nz/signup
